log:
  level: info

runner:
  file: /data/.runner
  capacity: 1
  envs: {}
  timeout: 3h
  insecure: false
  fetch_timeout: 5s
  fetch_interval: 2s
  labels:
    - "ubuntu-latest:docker://gitea/runner-images:ubuntu-latest"
    - "docker-push:host"
    - "self-hosted:host"

cache:
  enabled: true
  dir: ""
  host: ""
  port: 0

container:
  network: ""
  options:
  privilege: false
  workdir_parent:
